import React from 'react'
import spinner from '../../asserts/spinner1.gif'

const Spinner = () => {
    return (
        <React.Fragment>
            <div>
                <img src={spinner} alt='spinner'/>
            </div>

        </React.Fragment>
            
        
    )
}

export default Spinner
